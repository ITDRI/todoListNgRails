{
  "name": "testTodos",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm i ng-app && cd ng-app && npm run build && npm run start"
  },
  "engines": {
    "node": "12.18.4",
    "npm": "6.14.6"
  }
}
