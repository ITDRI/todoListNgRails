{
  "name": "testTodos",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm install ./ng-app",
    "start": "npm run --prefix ./ng-app build && node ./ng-app/server.js"
  },
  "engines": {
    "node": "12.x",
    "npm": "6.14.6"
  }
}
